@import './src/app/common';

.button {
  width: 7em;
  border: 0;
  background: $harbor;
  padding: $normalGap;
}

.buttons {
  width: 100%;
  display: flex;
  flex-direction: row;
  justify-content: center;
}